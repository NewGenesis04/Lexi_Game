{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "/app/.venv/bin/uvicorn packages.backend-api.src.backend_api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
